# uni-app 模板的 peer 依赖存在已知冲突：
#   pinia 有一个【可选】peer @vue/composition-api（仅服务 Vue 2），
#   它声明 vue >= 2.5 < 2.7，与项目的 vue 3.4.21 冲突。
#   该 peer 对本项目无意义（我们只用 Vue 3），故跳过 peer 校验。
# 参考《MeowHome-小程序迁移步骤.md》§12 记录 1.2。
legacy-peer-deps=true